{
    "version": 2,
    "builds": [
    {
        "src": "./index.js",
        "use": "@vercel/node"
    }
    ],
    "routes": [
        { "src": "/api/(.*)", "dest": "/api/$1.js" },
        { "src": "/uploads/(.*)", "dest": "/uploads/$1" },
        { "src": "/", "dest": "/index.js" }
    ]
}
